# Copia este archivo a ".env" (sin ".example") y pon tu connection string real de Neon.
# En Render, esta misma variable se configura desde el panel de "Environment", no en este archivo.
DATABASE_URL=postgresql://usuario:password@host.neon.tech/basedatos?sslmode=require
